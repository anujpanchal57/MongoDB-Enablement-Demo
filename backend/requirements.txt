# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config / validation ---
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- MongoDB (async driver with SRV support) ---
pymongo[srv]==4.16.0

# --- Embeddings ---
voyageai==0.4.0

# --- AWS (Bedrock chat) ---
boto3==1.43.29

# --- Feature 2: agentic memory (LangChain + LangGraph) ---
# Pinned to the resolved langchain 1.x set (compatible with voyageai's langchain-core).
langgraph==1.2.5
langgraph-checkpoint-mongodb==0.4.0
langchain-aws==1.5.1
langchain-core==1.4.7

# --- HTTP ---
httpx==0.28.1

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.2
mongomock==4.3.0
